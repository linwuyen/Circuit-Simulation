(function (global) {
  "use strict";
  const Models=global.CircuitPowerModelsV1;if(!Models)return;
  const measurements=[["scope","Scope physical Vout"],["adc","ADC count / AFE pin"],["pwm","PWM command vs pin"],["timing","SOC / ISR / LOAD timestamps"],["trip","CMPSS / Trip flags"],["host","Host command + age"]];
  const markup=()=>`<section class="power-live-stage" data-power-stage="7" hidden><div class="power-live-head"><div><span class="power-live-kicker">STAGE 8 · CAPSTONE DEBUG</span><h4>Unknown system：用 measurement 最大化 information gain</h4><p>不是背 checklist，而是讓每一次 measurement 真正縮小 hypothesis space。</p></div><span class="power-live-same-machine">SYNTHETIC DIAGNOSTIC REASONING</span></div><div class="debug-case-head"><div><span>UNKNOWN FAULT</span><strong data-debug-symptom></strong></div><button type="button" data-debug-new>下一個 fault</button></div><div class="debug-hypotheses"><span>HYPOTHESIS SPACE</span><div>${Models.HYPOTHESES.map(h=>`<b data-hypothesis="${h}">${h}</b>`).join("")}</div></div><div class="debug-measurements"><strong>下一個要量什麼？</strong><div>${measurements.map(([id,label])=>`<button type="button" data-debug-measure="${id}">${label}</button>`).join("")}</div></div><div class="debug-evidence" data-debug-evidence><span>EVIDENCE LOG</span><p>尚未量測。</p></div><div class="debug-score"><div><span>Remaining hypotheses</span><strong data-debug-count></strong></div><div><span>Uniform-prior elimination gain</span><strong data-debug-quality></strong></div><button type="button" data-debug-reveal>Reveal root cause</button></div><div class="debug-root" data-debug-root hidden><span>ROOT CAUSE</span><strong></strong><p></p></div><div class="debug-checklist"><span>TRANSFER CHECKLIST</span><ol><li>能量怎麼流？</li><li>u / y 是什麼？</li><li>MCU 看見什麼？</li><li>何時 sample / actuate？</li><li>P(s) 有什麼限制？</li><li>Safety 如何 veto？</li><li>哪個 measurement 最大幅度縮小 hypothesis space？</li></ol></div><p class="power-boundary"><b>Reasoning fidelity：</b>這是 deterministic synthetic case bank。bits = log2(Nbefore/Nafter) 只在「候選 root cause 等先驗、measurement deterministic elimination」假設下等同 information gain；不是從實板資料估出的 Bayesian posterior。</p></section>`;
  function mount(root,panel){const symptom=panel.querySelector("[data-debug-symptom]"),evidence=panel.querySelector("[data-debug-evidence]"),count=panel.querySelector("[data-debug-count]"),quality=panel.querySelector("[data-debug-quality]"),rootCard=panel.querySelector("[data-debug-root]"),next=panel.querySelector("[data-debug-new]"),reveal=panel.querySelector("[data-debug-reveal]"),buttons=Array.from(panel.querySelectorAll("[data-debug-measure]"));let index=0,candidates=[];const scenario=()=>Models.DEBUG_SCENARIOS[index%Models.DEBUG_SCENARIOS.length];function paint(){panel.querySelectorAll("[data-hypothesis]").forEach(n=>{const alive=candidates.includes(n.dataset.hypothesis);n.classList.toggle("is-hot",alive&&candidates.length<=2);n.classList.toggle("is-dim",!alive);});count.textContent=String(candidates.length);}function reset(){candidates=Models.HYPOTHESES.slice();symptom.textContent=scenario().symptom;evidence.innerHTML="<span>EVIDENCE LOG</span><p>尚未量測。先選一個能切開最多 hypothesis 的 observation。</p>";quality.textContent="0% · 0.00 bits";rootCard.hidden=true;buttons.forEach(b=>b.disabled=false);paint();}
    buttons.forEach(b=>b.addEventListener("click",()=>{if(b.disabled)return;const result=Models.applyMeasurement(scenario(),candidates,b.dataset.debugMeasure);candidates=result.after;b.disabled=true;const p=document.createElement("p");p.innerHTML=`<b>${b.textContent}：</b>${result.before.length} → ${result.after.length} hypotheses`;evidence.appendChild(p);quality.textContent=`${result.reduction}% · ${result.bits.toFixed(2)} bits`;paint();}));reveal.addEventListener("click",()=>{const s=scenario();rootCard.hidden=false;rootCard.querySelector("strong").textContent=s.root;rootCard.querySelector("p").textContent=s.explanation;});next.addEventListener("click",()=>{index=(index+1)%Models.DEBUG_SCENARIOS.length;reset();});reset();return{reset};}
  global.CircuitPowerStagesV1=global.CircuitPowerStagesV1||{};global.CircuitPowerStagesV1.debug={index:7,markup,mount};
})(window);
