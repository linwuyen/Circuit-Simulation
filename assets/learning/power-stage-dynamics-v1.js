(function (global) {
  "use strict";
  const Store=global.CircuitPowerSystemStateV1, Models=global.CircuitPowerModelsV1;
  if(!Store||!Models)return;
  const fmt=Models.fmt;
  const markup=()=>`<section class="power-live-stage" data-power-stage="4" hidden><div class="power-live-head"><div><span class="power-live-kicker">STAGE 5 · DYNAMICS / SFRA</span><h4>同一個 loop，改用頻率鏡頭看</h4><p>把正弦擾動掃過頻率；delay 主要先吃 phase，真正 crossover 必須是 0 dB crossing。</p></div><span class="power-live-same-machine">SAME HARDWARE · LC SMALL-SIGNAL</span></div>
  <div class="power-predict"><strong>先猜：</strong><span>plant magnitude 幾乎不變，但額外增加 5 µs digital delay，phase margin 會？</span><button type="button" data-bode-predict="up">增加</button><button type="button" data-bode-predict="down">減少</button><button type="button" data-bode-predict="same">不變</button><button class="power-action" type="button" data-bode-inject>+5 µs delay</button><small data-bode-predict-status>純 delay 的 signature：magnitude 近似不變、phase 更落後。</small></div>
  <div class="power-control-grid"><label><span><b>Digital delay</b><output data-bode-delay-readout></output></span><input data-bode-delay type="range" min="0" max="25" step="0.5"></label><label><span><b>SFRA probe</b><output data-bode-probe-readout></output></span><input data-bode-probe type="range" min="0" max="100" step="1" value="50"></label></div>
  <div class="bode-grid"><article><div class="power-chart-head"><b>Loop magnitude</b><span>0 dB = true crossing</span></div><svg class="power-chart" viewBox="0 0 520 170"><path class="chart-grid" d="M0 42 H520 M0 85 H520 M0 128 H520"/><path data-bode-mag-path class="chart-line chart-mag"></path><line data-bode-mag-probe class="chart-probe" y1="0" y2="170"/></svg></article><article><div class="power-chart-head"><b>Loop phase</b><span>delay → −360 f Td</span></div><svg class="power-chart" viewBox="0 0 520 170"><path class="chart-grid" d="M0 42 H520 M0 85 H520 M0 128 H520"/><path data-bode-phase-path class="chart-line chart-phase"></path><line data-bode-phase-probe class="chart-probe" y1="0" y2="170"/></svg></article></div>
  <div class="power-metric-grid"><div><span>Probe gain</span><strong data-bode-probe-gain></strong></div><div><span>Probe phase</span><strong data-bode-probe-phase></strong></div><div><span>Crossover</span><strong data-bode-cross></strong></div><div><span>Phase margin</span><strong data-bode-pm></strong></div></div>
  <div class="sfra-diagnosis"><span>SFRA DEBUG SIGNATURE</span><strong data-bode-status></strong><p data-sfra-summary></p></div><p class="power-boundary"><b>Model fidelity L5：</b>CCM Buck LC + ESR small-signal model；0 / 1 / multiple crossings 分開判定，不用「最接近 0 dB」冒充 crossover。</p></section>`;
  function mount(root,panel){
    const delay=panel.querySelector("[data-bode-delay]"),probe=panel.querySelector("[data-bode-probe]"),inject=panel.querySelector("[data-bode-inject]"),status=panel.querySelector("[data-bode-predict-status]"),buttons=Array.from(panel.querySelectorAll("[data-bode-predict]"));let prediction=null,manualDelay=null;
    const text=(s,v)=>{const n=panel.querySelector(s);if(n)n.textContent=v;};
    function currentDelay(){return manualDelay==null?Models.timingState(Store.snapshot()).actuation:manualDelay;}
    function render(){const state=Store.snapshot(),d=currentDelay(),analysis=Models.analyzeLoop(state,d),probeNorm=Number(probe.value)/100,probeFreq=100*Math.pow(500,probeNorm),probePoint=Models.buckLoopPoint(probeFreq,d,state),phases=analysis.phases,mags=analysis.mags;delay.value=d;text("[data-bode-delay-readout]",`${fmt(d,1)} µs`);text("[data-bode-probe-readout]",probeFreq>=1000?`${fmt(probeFreq/1000,2)} kHz`:`${Math.round(probeFreq)} Hz`);text("[data-bode-probe-gain]",`${fmt(probePoint.magDb,1)} dB`);text("[data-bode-probe-phase]",`${fmt(probePoint.phase,1)}°`);if(analysis.status==="SINGLE"){const c=analysis.crossings[0];text("[data-bode-cross]",`${fmt(c.freq/1000,2)} kHz`);text("[data-bode-pm]",`${fmt(c.phaseMargin,1)}°`);text("[data-bode-status]","Single 0 dB crossing · PM 可解讀");}else if(analysis.status==="MULTIPLE"){text("[data-bode-cross]",`${analysis.crossings.length} crossings`);text("[data-bode-pm]","WARNING");text("[data-bode-status]","MULTIPLE CROSSOVER · 先處理 resonance / loop shape");}else{text("[data-bode-cross]","none");text("[data-bode-pm]","—");text("[data-bode-status]","NO 0 dB CROSSING · bandwidth 尚未成立");}text("[data-sfra-summary]",`At ${probeFreq>=1000?fmt(probeFreq/1000,2)+" kHz":Math.round(probeFreq)+" Hz"}，delay-only phase ≈ ${fmt(-360*probeFreq*d*1e-6,1)}°。`);panel.querySelector("[data-bode-mag-path]").setAttribute("d",Models.linePath(mags,520,170,-50,50));const pmin=Math.min(-90,...phases)-10,pmax=Math.max(0,...phases)+10;panel.querySelector("[data-bode-phase-path]").setAttribute("d",Models.linePath(phases,520,170,pmin,pmax));const x=probeNorm*520;[panel.querySelector("[data-bode-mag-probe]"),panel.querySelector("[data-bode-phase-probe]")].forEach(n=>{n.setAttribute("x1",x);n.setAttribute("x2",x);});return analysis;}
    delay.addEventListener("input",()=>{manualDelay=Number(delay.value);render();});probe.addEventListener("input",render);buttons.forEach(b=>b.addEventListener("click",()=>{prediction=b.dataset.bodePredict;buttons.forEach(x=>x.classList.toggle("is-selected",x===b));status.textContent="Prediction 已鎖定；按『+5 µs delay』。";}));inject.addEventListener("click",()=>{const before=render();const beforePm=before.status==="SINGLE"?before.crossings[0].phaseMargin:null;manualDelay=Models.clamp(currentDelay()+5,0,25);const after=render(),afterPm=after.status==="SINGLE"?after.crossings[0].phaseMargin:null;const actual=beforePm!=null&&afterPm!=null?(afterPm<beforePm?"down":afterPm>beforePm?"up":"same"):"down";status.textContent=`${prediction?(prediction===actual?"✓":"✕"):"觀察"} pure delay 增加 phase lag；crossing 狀態=${after.status}.`;status.classList.toggle("is-pass",prediction===actual);status.classList.toggle("is-fail",!!prediction&&prediction!==actual);});
    const unsub=Store.subscribe((_,c)=>{if(c.path.startsWith("control.")||c.path.startsWith("plant.")||c.path.startsWith("timing.")){if(c.path.startsWith("timing."))manualDelay=null;render();}});render();return{render,destroy:unsub};}
  global.CircuitPowerStagesV1=global.CircuitPowerStagesV1||{};global.CircuitPowerStagesV1.dynamics={index:4,markup,mount};
})(window);
