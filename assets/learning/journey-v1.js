(function (global) {
  "use strict";
  const Learning=global.CircuitLearning, Evidence=global.CircuitEvidence, Store=global.CircuitPowerSystemStateV1;
  if(!Learning||!Learning.renderHome||!Learning.curriculum)return;
  const previousRenderHome=Learning.renderHome,curriculum=Learning.curriculum;
  const moduleById=curriculum.moduleById||Object.fromEntries((curriculum.modules||[]).map(m=>[m.id,m]));
  const stages=[
    {id:"energy",number:"01",tag:"POWER PHYSICS",title:"能量：先看 Power Stage",question:"開關到底怎麼改變電壓、電流與能量？",why:"先從 ON/OFF、電感電流與輸出能量建立直覺。",modules:["buck"],active:["actuator","plant","output"],timeline:["PWM switching","L / C energy","Load"],system:"先證明能量怎麼流，再談控制。",model:"L1 · Ideal steady-state"},
    {id:"sensing",number:"02",tag:"SENSING",title:"量測：MCU 到底看見什麼",question:"真實的 A / V，怎麼一路變成 ADC count？",why:"Scale、offset、OPA、ADC 任一段錯，controller 就會相信錯誤世界。",modules:["adc"],active:["output","sensor","adc"],timeline:["Physical y","Sensor / scale","ADC count"],system:"量到什麼，controller 就相信什麼。",model:"L2 · Measurement model"},
    {id:"timing",number:"03",tag:"REAL-TIME",title:"時序：Sample 到 Actuate",question:"ADC 何時量？新 duty 又何時真的進入 plant？",why:"SOC、S/H、EOC、ISR/CLA、control 與 PWM shadow load 必須在同一條 deadline。",modules:["power-sync","loop10us"],active:["adc","controller","actuator"],timeline:["PWM SOC","ADC EOC","ISR / C(z)","PWM LOAD"],system:"算完不等於生效；錯過 load point 就多一拍。",model:"L3 · Discrete timing"},
    {id:"control",number:"04",tag:"FEEDBACK",title:"閉迴路：PI 為什麼能控制",question:"Reference 和 feedback 差多少，controller 應該怎麼推？",why:"先看 error、輸出與 transient，再把 Kp / Ki 翻成 loop shaping。",modules:["pi"],active:["reference","controller","actuator","plant","output","sensor","adc"],timeline:["r − y","PI / C(z)","u","P(s)","feedback"],system:"控制器只是用 error 產生 command。",model:"L4 · Control surrogate"},
    {id:"dynamics",number:"05",tag:"DYNAMICS",title:"動態：Bode、Pole、Delay",question:"為什麼看似正確的 PI，到了高頻卻會震？",why:"Laplace / Z / Bode / SFRA 是同一個 loop 的不同鏡頭。",modules:["control-transforms"],active:["controller","actuator","plant","output","sensor","adc"],timeline:["P(s)","Bode","C(z)","delay","SFRA"],system:"模型與實機差異，要能被量成 gain / phase / delay。",model:"L5 · LC small-signal"},
    {id:"topology",number:"06",tag:"TOPOLOGY",title:"拓撲：同骨架，不同 Plant",question:"Buck、PFC、PSFB、LLC 的『油門』為什麼不一樣？",why:"保留 feedback grammar，只替換 PlantDefinition。",modules:["power-topology-control","control-unification"],active:["actuator","plant","output","sensor"],timeline:["Duty / phase / fsw","Plant personality","Bandwidth boundary"],system:"同一套理論，不代表同一個 plant。",model:"L6 · Plant personality"},
    {id:"safety",number:"07",tag:"SAFETY",title:"安全：Protection 有否決權",question:"Fault 發生後，最短多久能讓能量停止？",why:"hardware trip 必須能直接否決 PWM，state 決定是否允許 re-arm。",modules:["protection"],active:["safety","actuator","plant"],timeline:["Fault","CMPSS / filter","Trip","PWM OFF"],system:"Safety plane 可以凌駕 control command。",model:"L7 · Safety state machine"},
    {id:"system",number:"08",tag:"INTEGRATION",title:"整機：Debug Unknown System",question:"Vout 不對時，下一個最有資訊量的 measurement 是什麼？",why:"把 sensing、timing、control、state、data ownership 與 evidence 串成同一台系統。",modules:["power-capstone"],active:["reference","controller","actuator","plant","output","sensor","adc","safety"],timeline:["Signal","Timing","Data","State","Control","Plant","Evidence"],system:"真正能力是遇到陌生系統仍能逐層證偽。",model:"L8 · Diagnostic evidence"}
  ];
  const esc=v=>String(v==null?"":v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"})[c]);
  const modules=ids=>ids.map(id=>moduleById[id]).filter(Boolean);
  function evidence(stage,state){const items=modules(stage.modules).flatMap(m=>[...(m.lessons||[]),...(m.labs||[])]);if(!items.length||!Evidence||typeof Evidence.evidenceLevel!=="function")return{done:0,total:items.length,percent:0};const done=items.filter(i=>Evidence.evidenceLevel(state,i.id)>=2).length;return{done,total:items.length,percent:Math.round(done*100/items.length)};}
  function stageCard(stage,index,state){const p=evidence(stage,state),m=modules(stage.modules)[0];return `<article class="journey-stage ${index===0?"is-active":""}" data-journey-stage="${index}" tabindex="0"><div class="journey-stage-top"><span class="journey-stage-num">${stage.number}</span><span class="tag">${stage.tag}</span><span class="journey-stage-progress">${p.total?`${p.done}/${p.total} practiced`:"open path"}</span></div><h3>${stage.title}</h3><p class="journey-stage-question">${stage.question}</p><p class="muted">${stage.why}</p><div class="journey-progress"><i style="width:${p.percent}%"></i></div><a class="button journey-enter" href="${esc(m?m.entry:"#")}">進入這一層 →</a></article>`;}
  function node(id,symbol,title,sub){return `<div class="journey-system-node" data-system-node="${id}"><span>${symbol}</span><b>${title}</b><small>${sub}</small></div>`;}
  function markup(state){return `<section class="journey-shell"><div class="journey-section-head"><div><p class="journey-eyebrow">POWER FIRMWARE JOURNEY · ONE MACHINE, EIGHT LAYERS</p><h2>不是學 19 個主題，而是把同一台數位電源一層一層建起來</h2><p>Same hardware，different model fidelity：每一層只保留回答當前問題所需的最小物理。</p></div><button class="button primary" id="journeyPlay" type="button">▶ 逐步播放</button></div><div class="journey-layout"><div class="journey-stage-list">${stages.map((s,i)=>stageCard(s,i,state)).join("")}</div><aside class="journey-system"><div class="journey-system-sticky"><div class="journey-system-head"><div><span class="tag blue">STICKY MENTAL MODEL</span><h3 id="journeySystemTitle"></h3></div><span class="journey-live-dot">LIVE</span></div><div class="journey-model-fidelity"><span>MODEL FIDELITY</span><strong id="journeyModelLevel"></strong></div><div class="journey-loop">${node("reference","r","Reference","target")}<span class="journey-arrow">→</span>${node("controller","C(z)","Controller","error → command")}<span class="journey-arrow">→</span>${node("actuator","u","Actuator","PWM / phase / fsw")}<span class="journey-arrow">→</span>${node("plant","P(s)","Power Stage","energy + dynamics")}<span class="journey-arrow">→</span>${node("output","y","Output","V / I / power")}</div><div class="journey-feedback">${node("sensor","H","Sensor / Scale","physical → volts")}<span class="journey-arrow">→</span>${node("adc","ADC","Sampling","volts → counts")}<span class="journey-feedback-return">↖ feedback to error</span></div><div class="journey-safety" data-system-node="safety"><span>SAFETY VETO</span><b>CMPSS / Trip / State → PWM OFF</b><small>Protection is not a normal software block.</small></div><div class="journey-system-explain"><span id="journeySystemTag"></span><strong id="journeySystemQuestion"></strong><p id="journeySystemMeaning"></p></div><div class="journey-timeline" id="journeyTimeline"></div></div></aside></div></section>`;}
  function applyStage(index,root){const stage=stages[index];if(!stage)return;root.querySelectorAll("[data-journey-stage]").forEach((c,i)=>c.classList.toggle("is-active",i===index));root.querySelectorAll("[data-system-node]").forEach(n=>{const id=n.dataset.systemNode;n.classList.toggle("is-active",stage.active.includes(id));n.classList.toggle("is-dim",!stage.active.includes(id));});root.querySelector("#journeySystemTitle").textContent=`${stage.number} · ${stage.title}`;root.querySelector("#journeyModelLevel").textContent=stage.model;root.querySelector("#journeySystemTag").textContent=stage.tag;root.querySelector("#journeySystemQuestion").textContent=stage.question;root.querySelector("#journeySystemMeaning").textContent=stage.system;root.querySelector("#journeyTimeline").innerHTML=stage.timeline.map(x=>`<span>${x}</span>`).join("<i>→</i>");if(Store)Store.set("ui.activeStage",index,{source:"journey"});}
  function bind(root){let timer=null,active=0;const play=root.querySelector("#journeyPlay"),stop=()=>{if(timer)global.clearInterval(timer);timer=null;if(play)play.textContent="▶ 逐步播放";};const select=i=>{active=i;applyStage(i,root);};root.querySelectorAll("[data-journey-stage]").forEach((card,i)=>{card.addEventListener("focusin",()=>{stop();select(i);});card.addEventListener("click",e=>{if(e.target.closest("a"))return;stop();select(i);});});if(play)play.addEventListener("click",()=>{if(timer){stop();return;}play.textContent="■ 停止播放";select(active);timer=global.setInterval(()=>select(active=(active+1)%stages.length),1500);});select(0);}
  function enhance(rootId){const root=document.getElementById(rootId),main=root&&root.querySelector("main"),hero=main&&main.querySelector(".hero");if(!main||!hero||main.querySelector(".journey-shell"))return;hero.classList.add("journey-hero");hero.innerHTML=`<div class="eyebrow">DIGITAL POWER FIRMWARE · FIRST PRINCIPLES</div><h1>從能量流，一路走到 C2000 閉迴路與整機 Debug</h1><p class="lead">同一台 converter，逐層加入 sensing、sampling、control、actuation、protection 與 diagnosis。</p><div class="journey-hero-chain"><span>Power</span><i>→</i><span>Sense</span><i>→</i><span>Sample</span><i>→</i><span>Control</span><i>→</i><span>Actuate</span><i>→</i><span>Protect</span><i>→</i><span>Debug</span></div>`;hero.insertAdjacentHTML("afterend",markup(Learning.loadState?Learning.loadState():{}));bind(root);
    const heads=Array.from(main.querySelectorAll(".section-head"));
    const libraryHead=heads.find(head=>{const h2=head.querySelector("h2");return h2&&h2.textContent.trim()==="完整主題";});
    if(libraryHead){const h2=libraryHead.querySelector("h2"),p=libraryHead.querySelector("p");h2.textContent="完整內容索引";if(p)p.textContent="Core Path 之外的 module 仍完整保留；這裡是查找入口，不再代表建議學習順序。";const grid=libraryHead.nextElementSibling;if(grid)grid.classList.add("journey-topic-library");}
    const notice=main.querySelector(".notice");if(notice&&!notice.querySelector(".journey-contract"))notice.insertAdjacentHTML("beforeend",` <span class="journey-contract">Journey 只改 teaching order；既有 canonical ID、evidence history、oracle 與 URL contract 不變。</span>`);
  }
  Learning.renderHome=function renderJourney(rootId){previousRenderHome(rootId);enhance(rootId);};
  global.CircuitJourneyV1={stages,applyStage,enhance};
})(window);