(function (global) {
  "use strict";

  const Learning = global.CircuitLearning;
  const Store = global.CircuitPowerSystemStateV1;
  const Models = global.CircuitPowerTeachingModelsV2;
  if (!Learning || typeof Learning.renderHome !== "function" || !Store || !Models) return;

  const previousRenderHome = Learning.renderHome;
  const fmt = (v, d) => Number(v).toFixed(d == null ? 2 : d);
  const text = (root, selector, value) => { const node = root.querySelector(selector); if (node) node.textContent = value; };

  function contractMarkup() {
    return `<section class="power-contract-v2" data-v2-contract><div class="power-v2-head"><div><span class="power-v2-kicker">SYSTEM CONTRACT · BEFORE THE CONTROL LOOP</span><h3>先定義「什麼叫成功」，才有 error、deadline 與 protection threshold</h3><p>規格不是文件尾頁；它是每一層模型、控制與驗證的共同 boundary condition。</p></div><span class="power-v2-badge">PRODUCT VIEW</span></div><div class="power-contract-grid" data-v2-contract-grid></div><div class="power-plane-grid"><article><span>ENERGY PLANE</span><b>Vin → switch → L/C → load</b><small>能量實際往哪裡流？</small></article><article><span>INFORMATION PLANE</span><b>physical → sensor → ADC → controller</b><small>MCU 到底相信什麼？</small></article><article><span>AUTHORITY PLANE</span><b>state / limiter / trip → PWM enable or veto</b><small>現在誰有控制權？</small></article><article><span>TIME & OWNERSHIP</span><b>sample age · writer · reader · commit time</b><small>這個值是誰的、多久以前的？</small></article></div></section>`;
  }

  function stage0Markup() { return `<section class="power-v2-card" data-v2-region-card><div class="power-v2-card-head"><div><span>OPERATING REGION</span><strong>先問模型現在還成立嗎？</strong></div><b data-v2-region></b></div><label class="power-v2-slider"><span>Load resistance <output data-v2-load-readout></output></span><input data-v2-load type="range" min="2" max="120" step="1"></label><div class="power-v2-region-map" data-v2-region-map><span data-region="DCM">DCM</span><span data-region="CCM">CCM</span><span data-region="CURRENT_LIMIT">CURRENT LIMIT</span><span data-region="SATURATION">DUTY SATURATION</span></div><div class="power-v2-metrics"><div><span>Iavg</span><b data-v2-region-iavg></b></div><div><span>CCM boundary</span><b data-v2-region-boundary></b></div><div><span>ΔIL</span><b data-v2-region-ripple></b></div><div><span>Equation validity</span><b data-v2-region-valid></b></div></div><p data-v2-region-reason></p></section>`; }

  function stage1Markup() { return `<section class="power-v2-card" data-v2-resolution><div class="power-v2-card-head"><div><span>RESOLUTION BUDGET</span><strong>不是所有 regulation error 都能靠 PI 消掉</strong></div><b>ADC ↔ PWM</b></div><label class="power-v2-slider"><span>ePWM TBPRD <output data-v2-tbprd-readout></output></span><input data-v2-tbprd type="range" min="500" max="5000" step="100"></label><div class="power-v2-metrics"><div><span>ADC LSB @ output</span><b data-v2-adc-lsb></b></div><div><span>PWM duty LSB</span><b data-v2-pwm-lsb></b></div><div><span>PWM equivalent Vout step</span><b data-v2-pwm-vstep></b></div><div><span>Quantization floor</span><b data-v2-resolution-floor></b></div></div><p>Observation resolution 與 actuator resolution 共同形成底限；controller 無法命令一個硬體不存在的中間值。</p></section>`; }

  function stage2Markup() { return `<section class="power-v2-card" data-v2-sampling><div class="power-v2-card-head"><div><span>SAMPLING PHYSICS</span><strong>ADC 讀到的是某個 switching phase 的 sample，不是抽象「真值」</strong></div><b data-v2-sample-phase></b></div><label class="power-v2-slider"><span>Sampling jitter <output data-v2-jitter-readout></output></span><input data-v2-jitter type="range" min="0" max="200" step="5"></label><label class="power-v2-slider"><span>PWM-synchronous SOC <output data-v2-alias-readout></output></span><input data-v2-sync type="checkbox"></label><div class="power-v2-metrics"><div><span>Sampled iL</span><b data-v2-sampled-il></b></div><div><span>Average iL</span><b data-v2-average-il></b></div><div><span>Ripple sampling error</span><b data-v2-ripple-error></b></div><div><span>Jitter uncertainty</span><b data-v2-jitter-band></b></div></div><div class="power-v2-causal"><b>switch ripple</b><i>→</i><b>SOC phase</b><i>→</i><b>S/H aperture</b><i>→</i><b>ADC sample</b><i>→</i><b>controller belief</b></div><p data-v2-settling></p></section>`; }

  function stage3Markup() { return `<section class="power-v2-card" data-v2-product-control><div class="power-v2-card-head"><div><span>PRODUCT CONTROL REALITY</span><strong>C(z) 之外還有 authority、limiter、anti-windup 與 feed-forward</strong></div><b data-v2-control-mode></b></div><div class="power-v2-control-chain"><span>Vref</span><i>→</i><span>Voltage loop</span><i>→</i><span>Iref limiter</span><i>→</i><span>Current authority</span><i>→</i><span>Duty limiter</span><i>→</i><span>Plant</span></div><div class="power-v2-two-controls"><label><span>Load R <output data-v2-cccv-load-readout></output></span><input data-v2-cccv-load type="range" min="1" max="12" step="0.25"></label><label><span>Current limit <output data-v2-current-limit-readout></output></span><input data-v2-current-limit type="range" min="2" max="15" step="0.5"></label></div><div class="power-v2-metrics"><div><span>Authority</span><b data-v2-cccv-mode></b></div><div><span>Physical target</span><b data-v2-cccv-v></b></div><div><span>Iout</span><b data-v2-cccv-i></b></div><div><span>Required duty</span><b data-v2-cccv-duty></b></div></div><div class="power-v2-toggle-grid"><label><input data-v2-aw type="checkbox"> Anti-windup</label><div><span>Vin sag → recovery</span><b data-v2-aw-result></b><small data-v2-aw-compare></small></div><label><input data-v2-ff type="checkbox"> Vin feed-forward</label><div><span>Vin step response</span><b data-v2-ff-result></b><small data-v2-ff-compare></small></div><label><input data-v2-bumpless type="checkbox"> Bumpless CC↔CV</label><div><span>Authority handoff</span><b data-v2-bumpless-result></b><small>preload incoming controller state to outgoing command</small></div></div><p>Feedback 修 residual error；feed-forward 預先補償已知 disturbance。CC/CV 切換不是「兩個 PI 同時搶 duty」，而是明確的 authority handoff。</p></section>`; }

  function stage4Markup() { return `<section class="power-v2-card" data-v2-bandwidth><div class="power-v2-card-head"><div><span>CASCADED-LOOP BANDWIDTH</span><strong>內環要先收斂，外環才看見近似受控的 plant</strong></div><b data-v2-separation></b></div><div class="power-v2-metrics"><div><span>Inner current loop target</span><b data-v2-inner-bw></b></div><div><span>Outer voltage loop target</span><b data-v2-outer-bw></b></div><div><span>Sample→actuate delay</span><b data-v2-loop-delay></b></div><div><span>Delay phase @ inner</span><b data-v2-inner-phase></b></div></div><p>「內環 5× 外環」不是宗教規則，而是在 plant coupling、delay 與 phase margin 之間留下可驗證的 separation。</p></section>`; }

  function stage5Markup() { return `<section class="power-v2-card" data-v2-plant-regions><div class="power-v2-card-head"><div><span>OPERATING-POINT DEPENDENT PLANT</span><strong>Topology 名稱不等於固定 P(s)</strong></div><b data-v2-plant-name></b></div><div class="power-v2-metrics"><div><span>Operating axes</span><b data-v2-plant-axis></b></div><div class="wide"><span>Region library</span><b data-v2-plant-region-list></b></div></div><p><b>Boundary：</b><span data-v2-plant-boundary></span></p><p>換 operating point 時，先重新問「哪個 model 還有效？」再搬 controller coefficient。</p></section>`; }

  function stage6Markup() { return `<section class="power-v2-card" data-v2-startup><div class="power-v2-card-head"><div><span>STARTUP / SHUTDOWN STATE MACHINE</span><strong>PI 正確也不能讓一個尚未 qualified 的系統進 RUN</strong></div><b data-v2-startup-state></b></div><div class="power-v2-metrics"><div><span>PWM permission</span><b data-v2-startup-pwm></b></div><div><span>Last guard</span><b data-v2-startup-guard></b></div><div class="wide"><span>State path</span><b>POWER_OFF → INIT → SELF_TEST → PRECHARGE → SOFT_START → RUN</b></div></div><div class="power-v2-qualifiers"><label><input type="checkbox" data-v2-qualifier="adcValid"> ADC valid</label><label><input type="checkbox" data-v2-qualifier="selfTestPass"> Self-test pass</label><label><input type="checkbox" data-v2-qualifier="busReady"> Vbus ready</label><label><input type="checkbox" data-v2-qualifier="prechargeDone"> Precharge done</label><label><input type="checkbox" data-v2-qualifier="softStartComplete"> Soft-start done</label></div><div class="power-v2-actions"><button type="button" data-v2-startup-power>Power on</button><button type="button" data-v2-startup-advance>Advance state</button><button type="button" data-v2-startup-fault>Inject fault</button><button type="button" data-v2-startup-safe>Fault source clear</button><button type="button" data-v2-startup-clear>Qualified clear</button></div><p data-v2-startup-status>先 Power on；沒有 qualifier 就嘗試 advance，觀察 state machine 拒絕原因。</p></section>`; }

  function stage7Markup() {
    const instrumentChoices = Object.entries(Models.INSTRUMENTS).map(([id, item]) => `<label><input type="checkbox" data-v2-instrument="${id}"> ${item.label}</label>`).join("");
    const ladder = Models.verificationLadder().map(v=>`<article><span>${v.name}</span><b>${v.purpose}</b><small>${v.faults}</small></article>`).join("");
    return `<section class="power-v2-card" data-v2-observability><div class="power-v2-card-head"><div><span>DATA OWNERSHIP + OBSERVABILITY ARCHITECTURE</span><strong>Debug 能力是在寫 firmware 時就設計進去的</strong></div><b>8 SLOT BUDGET</b></div><div class="power-v2-ownership" data-v2-ownership-table></div><div class="power-v2-actions"><button type="button" data-v2-age-host>Age host command +250 ms</button><button type="button" data-v2-refresh-host>Receive fresh host command</button></div><div class="power-v2-instrumentation"><div><span>Choose diagnostic signals</span><b data-v2-instrument-score></b></div><div class="power-v2-instrument-grid">${instrumentChoices}</div><p data-v2-instrument-status></p></div><div class="power-v2-causal"><b>writer</b><i>→</i><b>version / timestamp</b><i>→</i><b>owner</b><i>→</i><b>reader</b><i>→</i><b>physical consequence</b></div><div class="power-v2-card-head power-v2-verify-head"><div><span>VERIFICATION LADDER</span><strong>每個 escaped fault 都應變成下一層 regression vector</strong></div><b>MODEL → SIL → HIL → BOARD</b></div><div class="power-plane-grid power-v2-verification">${ladder}</div></section>`;
  }

  function insertEnhancements(root) {
    const shell = root.querySelector(".journey-shell");
    if (shell && !shell.querySelector("[data-v2-contract]")) { const head = shell.querySelector(".journey-section-head"); if (head) head.insertAdjacentHTML("afterend", contractMarkup()); }
    const buck = root.querySelector("[data-buck-live]"); if (buck && !buck.querySelector("[data-v2-region-card]")) buck.insertAdjacentHTML("beforeend", stage0Markup());
    const sensing = root.querySelector("[data-sensing-live]"); if (sensing && !sensing.querySelector("[data-v2-resolution]")) sensing.insertAdjacentHTML("beforeend", stage1Markup());
    for (const [index, markup, sel] of [[2,stage2Markup,"[data-v2-sampling]"],[3,stage3Markup,"[data-v2-product-control]"],[4,stage4Markup,"[data-v2-bandwidth]"],[5,stage5Markup,"[data-v2-plant-regions]"],[6,stage6Markup,"[data-v2-startup]"],[7,stage7Markup,"[data-v2-observability]"]]) { const stage=root.querySelector(`[data-power-stage="${index}"]`); if(stage&&!stage.querySelector(sel))stage.insertAdjacentHTML("beforeend",markup()); }
  }

  function bind(root) {
    const load=root.querySelector("[data-v2-load]"),tbprd=root.querySelector("[data-v2-tbprd]"),jitter=root.querySelector("[data-v2-jitter]"),sync=root.querySelector("[data-v2-sync]"),cccvLoad=root.querySelector("[data-v2-cccv-load]"),currentLimit=root.querySelector("[data-v2-current-limit]"),aw=root.querySelector("[data-v2-aw]"),ff=root.querySelector("[data-v2-ff]"),bumpless=root.querySelector("[data-v2-bumpless]");
    if(load)load.addEventListener("input",()=>{Store.set("plant.load",Number(load.value),{source:"teaching-v2-region"});Store.set("plant.duty",Store.get("plant.duty"),{source:"teaching-v2-region-refresh"});});
    if(tbprd)tbprd.addEventListener("input",()=>Store.set("actuator.tbprd",Number(tbprd.value),{source:"teaching-v2-resolution"}));
    if(jitter)jitter.addEventListener("input",()=>Store.set("sampling.jitterNs",Number(jitter.value),{source:"teaching-v2-sampling"}));
    if(sync)sync.addEventListener("change",()=>Store.set("sampling.synchronous",sync.checked,{source:"teaching-v2-sync"}));
    if(cccvLoad)cccvLoad.addEventListener("input",()=>{Store.set("plant.load",Number(cccvLoad.value),{source:"teaching-v2-cccv"});Store.set("plant.duty",Store.get("plant.duty"),{source:"teaching-v2-cccv-refresh"});});
    if(currentLimit)currentLimit.addEventListener("input",()=>Store.set("limits.currentLimitA",Number(currentLimit.value),{source:"teaching-v2-current-limit"}));
    if(aw)aw.addEventListener("change",()=>Store.set("control.antiWindup",aw.checked,{source:"teaching-v2-aw"}));
    if(ff)ff.addEventListener("change",()=>Store.set("control.feedForward",ff.checked,{source:"teaching-v2-ff"}));
    if(bumpless)bumpless.addEventListener("change",()=>Store.set("control.bumpless",bumpless.checked,{source:"teaching-v2-bumpless"}));
    root.querySelectorAll("[data-v2-qualifier]").forEach(box=>box.addEventListener("change",()=>Store.set(`startup.${box.dataset.v2Qualifier}`,box.checked,{source:"teaching-v2-startup"})));
    const startupStatus=root.querySelector("[data-v2-startup-status]");
    function transition(event){const result=Models.startupTransition(Store.get("startup"),event);Store.set("startup",result.next,{source:`teaching-v2-startup-${event}`});if(startupStatus)startupStatus.textContent=result.blocked?`BLOCKED · ${result.blocked}`:`${event} → ${result.next.state}`;}
    const power=root.querySelector("[data-v2-startup-power]");if(power)power.addEventListener("click",()=>transition("power_on"));const advance=root.querySelector("[data-v2-startup-advance]");if(advance)advance.addEventListener("click",()=>transition("advance"));const fault=root.querySelector("[data-v2-startup-fault]");if(fault)fault.addEventListener("click",()=>transition("fault"));const safe=root.querySelector("[data-v2-startup-safe]");if(safe)safe.addEventListener("click",()=>{Store.set("startup.faultInput",false,{source:"teaching-v2-startup-safe"});if(startupStatus)startupStatus.textContent="Fault input cleared; latch/state still requires qualified clear.";});const clear=root.querySelector("[data-v2-startup-clear]");if(clear)clear.addEventListener("click",()=>transition("clear_fault"));
    const ageHost=root.querySelector("[data-v2-age-host]");if(ageHost)ageHost.addEventListener("click",()=>Store.set("data.vref.ageMs",Number(Store.get("data.vref.ageMs")||0)+250,{source:"teaching-v2-age"}));const refreshHost=root.querySelector("[data-v2-refresh-host]");if(refreshHost)refreshHost.addEventListener("click",()=>{Store.set("data.vref.ageMs",0,{source:"teaching-v2-refresh"});Store.set("data.vref.version",Number(Store.get("data.vref.version")||0)+1,{source:"teaching-v2-refresh"});});
    const instrumentStatus=root.querySelector("[data-v2-instrument-status]");root.querySelectorAll("[data-v2-instrument]").forEach(box=>box.addEventListener("change",()=>{const selected=Array.from(root.querySelectorAll("[data-v2-instrument]:checked")).map(n=>n.dataset.v2Instrument),slots=Number(Store.get("instrumentation.slots")||8);if(selected.length>slots){box.checked=false;if(instrumentStatus)instrumentStatus.textContent=`Slot budget full: only ${slots} signals can be retained.`;return;}Store.set("instrumentation.selected",selected,{source:"teaching-v2-instrumentation"});if(instrumentStatus)instrumentStatus.textContent=`${selected.length}/${slots} slots used.`;}));
  }

  function render(root) {
    const state=Store.snapshot(),activeStage=Number(Store.get("ui.activeStage")||0),contractGrid=root.querySelector("[data-v2-contract-grid]");if(contractGrid)contractGrid.innerHTML=Models.contractRows(state).map(row=>`<article class="${row.stages.includes(activeStage)?"is-relevant":""}"><span>${row.label}</span><b>${row.value}</b></article>`).join("");
    const regionCard=root.querySelector("[data-v2-region-card]");if(regionCard){const r=Models.buckRegion(state),load=regionCard.querySelector("[data-v2-load]");if(load)load.value=state.plant.load;text(regionCard,"[data-v2-load-readout]",`${fmt(state.plant.load,1)} Ω`);text(regionCard,"[data-v2-region]",r.region);text(regionCard,"[data-v2-region-iavg]",`${fmt(r.iavg)} A`);text(regionCard,"[data-v2-region-boundary]",`${fmt(r.boundaryA)} A`);text(regionCard,"[data-v2-region-ripple]",`${fmt(r.deltaI)} App`);text(regionCard,"[data-v2-region-valid]",r.idealRuleValid?"Vout≈D·Vin VALID":"MODEL CHANGE REQUIRED");text(regionCard,"[data-v2-region-reason]",r.reason);regionCard.querySelectorAll("[data-region]").forEach(n=>n.classList.toggle("is-active",n.dataset.region===r.region));}
    const resolution=root.querySelector("[data-v2-resolution]");if(resolution){const b=Models.resolutionBudget(state),input=resolution.querySelector("[data-v2-tbprd]");if(input)input.value=state.actuator.tbprd;text(resolution,"[data-v2-tbprd-readout]",`${state.actuator.tbprd} counts`);text(resolution,"[data-v2-adc-lsb]",`${fmt(b.adcLsbOutputV*1000,2)} mV`);text(resolution,"[data-v2-pwm-lsb]",`${fmt(b.dutyLsbPct,3)}%`);text(resolution,"[data-v2-pwm-vstep]",`${fmt(b.pwmEquivalentOutputV*1000,2)} mV`);text(resolution,"[data-v2-resolution-floor]`,`≈ ${fmt(b.effectiveFloorV*1000,2)} mV`);}
    const sampling=root.querySelector("[data-v2-sampling]");if(sampling){const s=Models.sampleInductorCurrent(state),input=sampling.querySelector("[data-v2-jitter]"),syncInput=sampling.querySelector("[data-v2-sync]");if(input)input.value=state.sampling.jitterNs;if(syncInput)syncInput.checked=state.sampling.synchronous!==false;text(sampling,"[data-v2-jitter-readout]",`${Math.round(state.sampling.jitterNs)} ns`);text(sampling,"[data-v2-alias-readout]",s.synchronous?"PHASE LOCKED":`ALIAS BEAT ≈ ${fmt(s.aliasBeatHz,0)} Hz`);text(sampling,"[data-v2-sample-phase]",`${fmt(s.phasePct,1)}% of Ts`);text(sampling,"[data-v2-sampled-il]",`${fmt(s.sampledA,3)} A`);text(sampling,"[data-v2-average-il]",`${fmt(s.averageA,3)} A`);text(sampling,"[data-v2-ripple-error]",`${s.rippleErrorA>=0?"+":""}${fmt(s.rippleErrorA,3)} A`);text(sampling,"[data-v2-jitter-band]`,`±${fmt(s.jitterBandA,4)} A`);text(sampling,"[data-v2-settling]",`${s.modelValid?"CCM sampling model valid":"MODEL WARNING · "+s.region+"：先回 Stage 1 重新選 operating model。"} Acquisition ${fmt(state.timing.acquisitionUs,2)} µs / source τ ${fmt(state.sampling.sourceTauUs,2)} µs → residual settling fraction ≈ ${fmt(s.settlingResidual*100,1)}%.`);}
    const control=root.querySelector("[data-v2-product-control]");if(control){const point=Models.ccCvPoint(state,state.plant.load),awNow=Models.simulateWindup(state,!!state.control.antiWindup),awOther=Models.simulateWindup(state,!state.control.antiWindup),ffNow=Models.simulateFeedForward(state,!!state.control.feedForward),ffOther=Models.simulateFeedForward(state,!state.control.feedForward),handoff=Models.bumplessHandoff(state,!!state.control.bumpless);const loadInput=control.querySelector("[data-v2-cccv-load]"),limitInput=control.querySelector("[data-v2-current-limit]"),awInput=control.querySelector("[data-v2-aw]"),ffInput=control.querySelector("[data-v2-ff]"),bumplessInput=control.querySelector("[data-v2-bumpless]");if(loadInput)loadInput.value=state.plant.load;if(limitInput)limitInput.value=state.limits.currentLimitA;if(awInput)awInput.checked=!!state.control.antiWindup;if(ffInput)ffInput.checked=!!state.control.feedForward;if(bumplessInput)bumplessInput.checked=!!state.control.bumpless;text(control,"[data-v2-cccv-load-readout]",`${fmt(state.plant.load,2)} Ω`);text(control,"[data-v2-current-limit-readout]",`${fmt(state.limits.currentLimitA,1)} A`);text(control,"[data-v2-control-mode]",point.mode);text(control,"[data-v2-cccv-mode]",`${point.mode} · ${point.limiter}`);text(control,"[data-v2-cccv-v]",`${fmt(point.targetV)} V`);text(control,"[data-v2-cccv-i]",`${fmt(point.currentA)} A`);text(control,"[data-v2-cccv-duty]",`${fmt(point.duty*100,1)}%`);text(control,"[data-v2-aw-result]",`${state.control.antiWindup?"ON":"OFF"} · overshoot ${fmt(awNow.overshootPct,1)}%`);text(control,"[data-v2-aw-compare]`,`comparison: ${state.control.antiWindup?"OFF":"ON"} → ${fmt(awOther.overshootPct,1)}%`);text(control,"[data-v2-ff-result]",`${state.control.feedForward?"ON":"OFF"} · droop ${fmt(ffNow.droopV,2)} V`);text(control,"[data-v2-ff-compare]`,`comparison: ${state.control.feedForward?"OFF":"ON"} → ${fmt(ffOther.droopV,2)} V`);text(control,"[data-v2-bumpless-result]",`${state.control.bumpless?"PRELOADED":"COLD"} · command jump ${fmt(handoff.commandJumpPct,2)}% duty`);}
    const bandwidth=root.querySelector("[data-v2-bandwidth]");if(bandwidth){const b=Models.loopBandwidthBudget(state);text(bandwidth,"[data-v2-separation]",`${fmt(b.separation,0)}× separation`);text(bandwidth,"[data-v2-inner-bw]",`${fmt(b.innerHz/1000,2)} kHz`);text(bandwidth,"[data-v2-outer-bw]",`${fmt(b.outerHz/1000,2)} kHz`);text(bandwidth,"[data-v2-loop-delay]",`${fmt(b.actuationUs,2)} µs`);text(bandwidth,"[data-v2-inner-phase]`,`−${fmt(b.phaseLagInnerDeg,1)}°`);}
    const plant=root.querySelector("[data-v2-plant-regions]");if(plant){const id=Store.get("ui.topologyPreview")||"buck",p=Models.plantRegion(id);text(plant,"[data-v2-plant-name]",String(id).toUpperCase());text(plant,"[data-v2-plant-axis]",p.axis);text(plant,"[data-v2-plant-region-list]",p.regions.join(" · "));text(plant,"[data-v2-plant-boundary]",p.boundary);}
    const startup=root.querySelector("[data-v2-startup]");if(startup){const view=Models.startupView(state.startup);text(startup,"[data-v2-startup-state]",view.state);text(startup,"[data-v2-startup-pwm]",view.pwmAllowed?"ALLOW":"BLOCKED");text(startup,"[data-v2-startup-guard]",state.startup.faultInput?"FAULT INPUT":"qualification gates");startup.querySelectorAll("[data-v2-qualifier]").forEach(box=>{box.checked=!!state.startup[box.dataset.v2Qualifier];});startup.classList.toggle("is-armed",view.pwmAllowed);}
    const obs=root.querySelector("[data-v2-observability]");if(obs){const table=obs.querySelector("[data-v2-ownership-table]");if(table)table.innerHTML=Object.entries(state.data).map(([name,record])=>{const fresh=Models.dataFreshness(record,record.maxAgeMs);return `<article class="${fresh.fresh?"":"is-stale"}"><span>${name.toUpperCase()}</span><b>${record.owner}</b><small>${record.writer} → ${record.reader}</small><em>${fmt(fresh.ageMs,3)} ms · v${record.version} · ${fresh.label}</em></article>`;}).join("");const selected=state.instrumentation.selected||[];obs.querySelectorAll("[data-v2-instrument]").forEach(box=>{box.checked=selected.includes(box.dataset.v2Instrument);});const score=Models.instrumentationScore(selected);text(obs,"[data-v2-instrument-score]",`${score.slots}/${state.instrumentation.slots} slots · ${score.score}% coverage`);const status=obs.querySelector("[data-v2-instrument-status]");if(status&&!status.textContent)status.textContent=score.missing.length?`Missing: ${score.missing.join(", ")}`:"All diagnostic dimensions covered.";}
  }

  function mount(root){if(!root||root.dataset.powerTeachingV2==="mounted")return;insertEnhancements(root);bind(root);render(root);const unsubscribe=Store.subscribe(()=>render(root));root.dataset.powerTeachingV2="mounted";global.CircuitPowerTeachingV2={render:()=>render(root),destroy:unsubscribe};}
  Learning.renderHome=function renderHomeWithTeachingV2(rootId){previousRenderHome(rootId);mount(document.getElementById(rootId));};
})(window);
